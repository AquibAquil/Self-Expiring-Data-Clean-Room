org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true

# OPTIONAL: If Gradle fails on Windows with "Unable to establish loopback connection"
# (a known JDK21 NIO Pipe bug that surfaces with Android Studio's bundled JBR),
# uncomment the line below and set it to your local JDK 17 install path.
# org.gradle.java.home=C:/Program Files/Eclipse Adoptium/jdk-17.0.18.8-hotspot
