name: company_agent
description: Clean room company-side agent. Hashes local identifiers and submits them to the clean room; reads back the aggregate result. One binary, --role a|b at launch.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.4.0

dependencies:
  at_client: ^3.11.0
  at_commons: ^5.0.0
  at_cli_commons: ^1.3.0
  crypto: ^3.0.3
