name: clean_room_computation
description: Waits for both companies' hashed submissions, computes the overlap, and publishes the aggregate result back to both companies.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.4.0

dependencies:
  at_client: ^3.11.0
  at_commons: ^5.0.0
  at_cli_commons: ^1.3.0
